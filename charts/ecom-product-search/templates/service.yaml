apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-search
spec:
  type: {{ .Values.service.type }}
  selector:
    app: search
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8082